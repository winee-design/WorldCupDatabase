A Database that was created by using a bash script "insert_data.sh"

This script pulled from a file named "games.csv" which was formatted like so:
year,round,winner,opponent,winner_goals,opponent_goals

After creating the database, I made a query script which automatically queried the database.
